#include "signal.hpp"

using std::copy;

/*
void IntegerSignal::put(empty_vec_t *v)
{
	int_vec_t* vv = reinterpret_cast< int_vec_t* >(v);
	copy(vv->begin(), vv->end(), data_array_);
}

void RealSignal::put(empty_vec_t *v)
{
	real_vec_t* vv = reinterpret_cast< real_vec_t* >(v);
	copy(vv->begin(), vv->end(), data_array_);
}

void ComplexSignal::put(empty_vec_t *v)
{
	complex_vec_t* vv = reinterpret_cast< complex_vec_t* >(v);
	copy(vv->begin(), vv->end(), data_array_);
}

void BitSignal::put(empty_vec_t *v)
{
	bit_vec_t* vv = reinterpret_cast< bit_vec_t* >(v);
	copy(vv->begin(), vv->end(), data_array_);
}

void StringSignal::put(empty_vec_t *v)
{
	string_vec_t* vv = reinterpret_cast< string_vec_t* >(v);
	copy(vv->begin(), vv->end(), data_array_);
}*/

/*
IntegerSignal::element_t * IntegerSignal::get_data()
{
	return data_array_;
}

RealSignal::element_t * RealSignal::get_data()
{
	return data_array_;
}

ComplexSignal::element_t * ComplexSignal::get_data()
{
	return data_array_;
}

BitSignal::element_t * BitSignal::get_data()
{
	return data_array_;
}

StringSignal::element_t * StringSignal::get_data()
{
	return data_array_;
}
*/
